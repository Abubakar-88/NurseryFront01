import { NurseryWorldValidators } from './nursery-world-validators';

describe('NurseryWorldValidators', () => {
  it('should create an instance', () => {
    expect(new NurseryWorldValidators()).toBeTruthy();
  });
});
